def display_horses(horses)
  horses.each_with_index do |horse, index|
    puts "#{index + 1}" - #{horse}"
  end
end
#le horse  pour que le nom s'affiche avec index tiret(on a modifier pour l'utilsateur pour que ca commence en 1)

